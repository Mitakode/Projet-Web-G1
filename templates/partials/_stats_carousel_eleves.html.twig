{#
    Partial: _stats_carousel_eleves.html.twig
    Purpose: Student-related statistics carousel.
    Data: Expects `title` and `stats` (from StatsModel).
#}
<section class="stats-section" data-carousel-scope>
    <div class="stats-header">
        <h2>{{ title|default('Students') }}</h2>
        <div class="stats-controls">
            <button class="stats-nav" type="button" data-carousel-prev aria-label="Previous">‹</button>
            <button class="stats-nav" type="button" data-carousel-next aria-label="Next">›</button>
        </div>
    </div>

    <div class="stats-carousel" data-carousel>
        <div class="stats-track" data-carousel-track>
            <article class="stat-card">
                <h3>Registered students</h3>
                <p class="stat-value">{{ stats.total_eleves|default(0) }}</p>
                <p class="stat-hint">Total number of students</p>
            </article>

            <article class="stat-card">
                <h3>Avg applications / student</h3>
                <p class="stat-value">{{ stats.avg_candidatures|default(0)|number_format(2, ',', ' ') }}</p>
                <p class="stat-hint">Applications per student</p>
            </article>

            <article class="stat-card">
                <h3>Top students (applications)</h3>
                {% if stats.top_candidatures is empty %}
                    <p class="stat-empty">No application recorded.</p>
                {% else %}
                    <ul class="stat-list">
                        {% for eleve in stats.top_candidatures %}
                            <li>
                                {{ eleve.Prenom }} {{ eleve.Nom }}
                                <span class="stat-badge">{{ eleve.total_candidatures }}</span>
                            </li>
                        {% endfor %}
                    </ul>
                {% endif %}
            </article>

            <article class="stat-card">
                <h3>Top students (wishlist)</h3>
                {% if stats.top_wishlist is empty %}
                    <p class="stat-empty">No wishlist available.</p>
                {% else %}
                    <ul class="stat-list">
                        {% for eleve in stats.top_wishlist %}
                            <li>
                                {{ eleve.Prenom }} {{ eleve.Nom }}
                                <span class="stat-badge">{{ eleve.total_wishlist }}</span>
                            </li>
                        {% endfor %}
                    </ul>
                {% endif %}
            </article>
        </div>
        <div class="stats-dots" data-carousel-dots aria-label="Carousel navigation"></div>
    </div>
</section>
