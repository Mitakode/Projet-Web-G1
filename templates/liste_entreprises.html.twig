{#
    Template: liste_entreprises.html.twig
    Purpose: Back-office company list with pagination.
    Data: Provided by DashboardController::listEntreprise.
#}
<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="utf-8">
    <title>Career Quest</title>
    <meta name="description" content="Liste des entreprises (back-office).">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="/assets/css/main-stylesheet.css" rel="stylesheet"/>
    <link href="/assets/css/liste_entreprise.css" rel="stylesheet"/>
</head>

<body>

{% include 'partials/_header.html.twig' %}

<main>
    <a href="/account" class="btn-back u-ml-10">Retour</a>
    <h2>Liste des entreprises ({{ totalElements|default(entreprises|length) }})</h2>
    <table>
        <thead>
            <tr>
                <th>Nom</th>
                <th>Description</th>
                <th>E-mail</th>
                <th>Téléphone</th>
                <th>Actif</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            {% for e in entreprises %}
            <tr>
                <td>{{ e.Nom }}</td>
                <td>{{ e.Description }}</td>
                <td>{{ e.Email_contact }}</td>
                <td>{{ e.Telephone|default('') }}</td>
                <td>{{ e.Est_actif == 1 ? 'Oui' : 'Non' }}</td>
                <td>
                    <a href="/entreprise?id={{ e.Id_entreprise }}" class="btn-edit">Modifier</a>
                    <form method="POST" action="/entreprise" class="inline-form"
                        onsubmit="return confirm('Supprimer {{ e.Nom }} ?')">
                        <input type="hidden" name="action" value="delete">
                        <input type="hidden" name="id" value="{{ e.Id_entreprise }}">
                        <button type="submit" class="btn-delete">Supprimer</button>
                    </form>
                </td>
            </tr>
            {% else %}
                <tr><td colspan="10">Aucune entreprise trouvée.</td></tr>
            {% endfor %}
        </tbody>
    </table>

    {% if totalPages is defined and totalPages > 1 %}
        <div class="pagination-container">
            {% if pageActuelle > 1 %}
                <a href="/enterprise_list?page={{ pageActuelle - 1 }}"
                   class="pagination-btn pagination-nav">
                    Précédent
                </a>
            {% endif %}

            {% for i in 1..totalPages %}
                <a href="/enterprise_list?page={{ i }}"
                   class="pagination-btn {{ i == pageActuelle ? 'is-active' : '' }}">
                    {{ i }}
                </a>
            {% endfor %}

            {% if pageActuelle < totalPages %}
                <a href="/enterprise_list?page={{ pageActuelle + 1 }}"
                   class="pagination-btn pagination-nav">
                    Suivant
                </a>
            {% endif %}
        </div>
    {% endif %}
</main>

{% include 'partials/_footer.html.twig' %}

</body>
</html>
